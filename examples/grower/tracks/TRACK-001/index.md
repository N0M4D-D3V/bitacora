# TRACK-001

Estado: `done` (`verification`, 2026-02-25).

Resumen:
- Implementado `PortfolioService` y `RiskService` para accounting y validaciones de riesgo MVP.
- Cobertura principal en `tests/unit/portfolio-service.test.ts` y `tests/unit/risk-service.test.ts`.
- Detalle canónico en `tracks/tracks.md` (sección TRACK-001).
